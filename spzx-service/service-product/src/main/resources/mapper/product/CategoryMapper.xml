<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--接口全类名-->
<mapper namespace="spzxproduct.Mapper.CategoryMapper">

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,name,image_url,parent_id,status,order_num,create_time,update_time,is_deleted
    </sql>
    <!--    //获取一级分类-->
    <select id="findOneCategory" resultType="com.atguigu.spzx.model.entity.product.Category">
            select <include refid="columns" />
            from category
            where
            parent_id = 0
            and status = 1
            and is_deleted = 0
            order by order_num
    </select>

<!--    //获取所有分类-->
    <select id="findAll" resultType="com.atguigu.spzx.model.entity.product.Category">
        select <include refid="columns" />
        from category
        where
        status = 1
        and is_deleted = 0
        order by order_num
    </select>


</mapper>

        <!--文件名要与java包中全类名文件名一样否则程序会找不到-->